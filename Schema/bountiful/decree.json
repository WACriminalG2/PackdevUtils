{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type":"object",
    "title": "Bountiful Decree",
    "description": "Decrees are items that determine which items show up in bounty boards.",
    "required": ["spawnsInBoard","objectivePools","rewardPools"],
    "properties": {
        "spawnsInBoard": {
            "type": "boolean",
            "description": "Whether this decree will naturally generate in bounty boards."
        },
        "objectivePools": {
            "type": "array",
            "description": "List of pools from which objectives will be generated by this decree.",
            "items": {
                "type": "string"
            }
        },
        "rewardPools": {
            "type": "array",
            "description": "List of pools from which rewards will be generated by this decree.",
            "items": {
                "type": "string"
            }
        }
    }
}